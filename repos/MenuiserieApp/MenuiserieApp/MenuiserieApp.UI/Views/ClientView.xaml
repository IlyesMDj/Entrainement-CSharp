<UserControl x:Class="MenuiserieApp.UI.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1160"
             Background="#F5F7FA">

    <UserControl.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ClientListItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Margin="{TemplateBinding Margin}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="BorderBrush" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Gestion de l'Atelier : Clients" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="#1E293B"
                   Margin="0,0,0,24"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Base Contacts" FontSize="16" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,12"/>

                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding ListeClients}"
                             SelectedItem="{Binding ClientSelectionne}"
                             ItemContainerStyle="{StaticResource ClientListItem}"
                             BorderThickness="0" Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Nom}" FontSize="15" FontWeight="SemiBold" Foreground="#1E293B"/>
                                        <TextBlock Text="{Binding RaisonSociale}" FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding Telephone}" FontSize="12" Foreground="#94A3B8"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="🗑" FontSize="14" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"
                                            Command="{Binding DataContext.SupprimerClientCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding Id}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Foreground" Value="#94A3B8"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="24">

                <TabControl Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border x:Name="Border" Padding="12,8" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="0,0,16,16" Cursor="Hand">
                                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#2563EB"/>
                                                <Setter Property="Foreground" Value="#2563EB"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="False">
                                                <Setter Property="Foreground" Value="#64748B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </TabControl.Resources>

                    <TabItem Header="➕ Nouveau Profil">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="Raison Sociale (Optionnel)" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.RaisonSociale, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Nom *" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.Nom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Prénom" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.Prenom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Téléphone" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.Telephone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Adresse Email" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Adresse Postale" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NouveauClient.Adresse, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap"/>
                                </StackPanel>
                            </ScrollViewer>

                            <Button Grid.Row="1" Content="✓ Enregistrer le Client" Padding="0,14" FontSize="15" FontWeight="Bold" Background="#10B981" Foreground="White" BorderThickness="0" Cursor="Hand"
                                    Command="{Binding EnregistrerClientCommand}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </TabItem>

                    <TabItem Header="✏️ Modifier la Sélection">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="Sélectionnez un contact dans la liste à gauche pour modifier ses informations." 
                                               Foreground="#94A3B8" FontStyle="Italic" Margin="0,0,0,16" TextWrapping="Wrap"/>

                                    <TextBlock Text="Raison Sociale (Optionnel)" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.RaisonSociale, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Nom *" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.Nom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Prénom" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.Prenom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Téléphone" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.Telephone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Adresse Email" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>

                                    <TextBlock Text="Adresse Postale" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ClientSelectionne.Adresse, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap"/>
                                </StackPanel>
                            </ScrollViewer>

                            <Button Grid.Row="1" Content="💾 Sauvegarder les modifications" Padding="0,14" FontSize="15" FontWeight="Bold" Background="#2563EB" Foreground="White" BorderThickness="0" Cursor="Hand"
                                    Command="{Binding EnregistrerModificationsCommand}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
    </Grid>
</UserControl>